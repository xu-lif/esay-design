/**
 *
 * FormCore:
 *   数据仓库，发布订阅模式，
 * FormField:
 *   收集数据, 以及config, 为组件绑定value,onchange
 *
 */

import Form from "./Form";
import FormField from "./FormField";
import useForm  from "./useForm";

export default Form
export { FormField, useForm }